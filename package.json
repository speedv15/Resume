{
  "name": "resume-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run export-md && vite build",
    "preview": "vite preview",
    "export-md": "node ./scripts/build-md.js",
    "dev:refresh": "bash ./scripts/refresh-dev.sh",
    "refresh:prod": "git pull --ff-only && npm ci && npm run build && npm run preview"
  },
  "dependencies": {
    "marked": "^5.1.1"
  },
  "devDependencies": {
    "vite": "^5.1.0"
  }
}